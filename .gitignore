# Runtime secrets/config (never commit)
backend/.env

# User uploads — keep the folder, ignore contents
backend/uploads/*
!backend/uploads/.gitkeep

# Build output (build.sh regenerates it)
backend/out/

# Misc
__pycache__/
*.pyc
.DS_Store
node_modules/
